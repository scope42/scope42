{
  "name": "scope42",
  "version": "0.0.0",
  "private": true,
  "license": "GPL-3.0-only",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "prettier": "@xinra/prettier-config",
  "devDependencies": {
    "@xinra/prettier-config": "2.0.0",
    "turbo": "1.5.6"
  }
}
